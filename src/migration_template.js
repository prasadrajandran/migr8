/**
 * @file This migration does the following:
 *   <describe what the migration does here>
 */

/**
 * Execute migration.
 *
 * @param {any} arg
 * @returns {Promise<any>}
 */
exports.up = async (arg) => {
  //
};

/**
 * Rollback migration.
 *
 * @param {any} arg
 * @returns {Promise<any>}
 */
exports.down = async (arg) => {
  //
};
